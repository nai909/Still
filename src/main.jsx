import React from 'react';
import ReactDOM from 'react-dom/client';
import Still from './App';
import './index.css';

// Create root and render
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Still />
  </React.StrictMode>
);
